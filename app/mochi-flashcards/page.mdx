import LinkTo from '@/components/LinkTo'
import Spacing from '@/components/Spacing'
import FlashcardDeck from '@/components/FlashcardDeck'
import 'katex/dist/katex.min.css'
import Latex from 'react-latex-next'
import Avatar, { Person } from '@/components/ui/avatar'
import LazyIndexModel from './LazyIndexModel'

export const metadata = {
  title: 'Making flashcards before I forget - Tomas Maillo',
  description:
    'How I connected an iPhone Shortcut, OpenAI and Mochi to turn passing questions into flashcards.',
}

<LinkTo displayText="Back" link="/" />
<Spacing lines={2} />

# Making flashcards before I forget

I keep coming across small things I want to remember: a word I have not seen before or a fact that feels useful but will probably be forgotten whenever I blink next. I hate this.

Bcak in uni I had lots of success with flashcards. I went quite deep into the idea of _space repetition_. Where you review the flashcards at increasing intervals. I had a custom setup with Obsidian and a forked copy of [obsidian-spaced-repetition](https://github.com/st3v3nmw/obsidian-spaced-repetition) by <Avatar person={Person.StephenMwangi} />. I was able to create my flashcards in the same document I had my lecture notes with inline <Latex children={"$\\LaTeX$"} /> support! 

Just for fun, here are a few examples of flashcards I made for uni exams:

<FlashcardDeck />

It worked! I credit these flashcards for my success in exams. Yet I tried to answer these questions while writing this blog post, but I must admit that they escape me. Spaced repetition depends on you coming back to the flashcards and recalling the answer! I did not do that enough.

Another thing to note about these flashcards is that they were made by hand. In the _before_ times! Before AI was good at all. Flashcards are a pain to study but they are double the pain to make. This started my journey into AI and the world of flashcards and this blog post to summarize it.


<div className="border border-card md:rounded-lg px-4 bg-card -mx-4">
<h2 className="text-lg font-medium pt-4">A fair argument against flashcards</h2>

While I have always raved about flashcards, I must say that they may not be the solution for everyone. 

My good friend <Avatar person={Person.CaterinaMammola} /> had been the complete oposite of me on this topic. We had a long discussion about it and I have always had this running joke about the downsides of flashcards vs mind maps. She [posts](https://caterina.codes/posts/make-mindmaps):

> I won't go into the science of mind maps too much because Justin Sung covers it much better but in a nutshell: 90% of learning is all about making connections to stuff. When you're learning a topic, you need to be able to distill it and understand how it connects to other topics, so that when you are asked a question about it, the correct relevant information can fire.
> This is why flashcards are bad - if the exam question is not the same as your flashcard, it's unlikely the correct knowledge in your brain will fire.

While I would never admit this to her, I do agree with her. Mind maps are better for building the shape of a subject: the relationships, parallels and dependencies between ideas. 

But the reality is that you only need relationships if you do not know the underlying knowledge. And the truth is that the world is not entirely interconnected. Sometimes a fact just *is* and connecting it is not useful. The moon's gravity is <Latex>{"$1.622\\ \\mathrm{m/s}^2$"}</Latex> and the [first website ever created](https://info.cern.ch/) was in 1991.

</div>

## How it works

Now

The visible part is an [iPhone Shortcut](https://support.apple.com/guide/shortcuts/welcome/ios). I give it a topic, such as `difference between encryption and hashing`, and it sends that text to a private API route on this website.


The endpoint first checks a secret token from the Shortcut. It rejects requests without the correct token, limits the topic length, cleans up any tags and chooses either the requested Mochi deck or my default one.

It then asks OpenAI for one or more cards. The prompt is deliberately strict: fronts should be clear, backs should be no longer than a sentence, duplicate cards should be avoided, and a simple topic should stay as a single card. The response must match a JSON schema containing only a `front` and `back`.

That structured response matters. Asking a model for "some flashcards" and hoping to parse whatever comes back would be fragile. The schema gives the rest of the script a predictable shape to validate before anything reaches my deck.

Finally, the route turns each result into Mochi's card format, attaches my template fields and optional tags, then calls the [Mochi API](https://mochi.cards/docs/api/). Cards are created one at a time because Mochi allows only one active request per account.

The endpoint returns the created card names and how long the whole process took. From my point of view, though, it is just:

1. Think of something worth remembering.
2. Run the Shortcut.
3. Carry on with my day.

## The part I like

There is nothing especially groundbreaking here. It is a short API route joining three existing tools together.

But it removes the exact bit of friction that kept stopping me. That is often my favourite kind of personal software: not a new platform or a grand system, just a tiny adjustment that makes the better habit easier than the lazy one.

The risk is that generating cards becomes so easy that I stop deciding what is worth learning. I am trying to avoid that. A neat automation can create a card, but it cannot care about the answer for me.

For now, the Shortcut has one job: catch a useful thought before I convince myself I will remember it.


## The ultimate goal 

The ultimate goal is to connect it to my Index 01. Not sure how well this work but we can always give it a go. 

The Index 01 is a smart ring that can record audio with a button press. 

<LazyIndexModel />

## Looking to make your own? 

My sister <Avatar person={Person.CrisMaillo} /> has made a very similar and public solution working with Claude. 
