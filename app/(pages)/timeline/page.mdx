import BackLink from '@/components/BackLink'
import Timeline from './timeline'
import Spacing from '@/components/Spacing'
import PostTitle from '@/components/PostTitle'
import TimelineIcon from '@/public/pixel-arts/timeline.svg'

export const metadata = {
  title: 'Timeline',
  description: 'A timeline of my projects',
}

<BackLink />
<Spacing lines={2} />

<PostTitle Icon={TimelineIcon}>Timeline</PostTitle>

I don't see GitHub contributions as a true measure of productivity, but they can be a surprisingly effective source of motivation. There's something powerful about looking back and seeing a consistent record of progress. 

A timeline of my projects and milestones over the years, with the occasional moment from the wider world alongside them:

<Timeline />
